{
  "name": "survivor-game-server",
  "version": "1.0.0",
  "description": "Multiplayer survivor game server on Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.22.0"
  }
}
